{% extends "base.html" %}

{% block content %}
    <h1>Enter your alternatives: </h1>
    <form action="" method="post" novalidate>
        {{ form.hidden_tag() }}
        <p>
			{{ form.alternative.label }}<br>
			{{ form.alternative() }}
		</p>
		<p>
			{{ form.alternative.label }}<br>
			{{ form.alternative() }}
		</p>
		<p>
			{{ form.add() }}
		</p>
		<p>
			{{ form.submit() }}
		</p>
    </form>
{% endblock %}